<?xml version="1.0"?>
<xs:schema xmlns:xs="http://www.w3.org/2001/XMLSchema" elementFormDefault="qualified">

    <xs:include schemaLocation="tiposBase.xsd" />

    <!-- Mensaje de Info Tiendas -->
    <xs:element name="mit">
        <xs:complexType>
            <xs:sequence>
                <xs:element name="infoMensaje" type="infoMensaje" />
                <xs:element name="listaTiendas" type="listaTiendas"/>
            </xs:sequence>
        </xs:complexType>
    </xs:element>
</xs:schema>