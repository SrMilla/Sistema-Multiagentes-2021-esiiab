<?xml version="1.0"?>
<xs:schema xmlns:xs="http://www.w3.org/2001/XMLSchema" elementFormDefault="qualified">

    <xs:include schemaLocation="tiposBase.xsd" />

    <!-- Mensaje de Arranca o Espera -->
    <!-- En verdad siempre es espera pq arrancará cuando reciba el MCI,
        por eso este mensaje solo lleva infoMensaje -->
    <xs:element name="mae">
        <xs:complexType>
            <xs:sequence>
                <xs:element name="infoMensaje" type="infoMensaje" />
            </xs:sequence>
        </xs:complexType>
    </xs:element>
</xs:schema>